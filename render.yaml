services:
  - type: web
    name: quizarena-node
    env: node
    plan: free
    autoDeploy: true
    envVars:
      - key: NODE_VERSION
        value: "18.17.0"
    buildCommand: "npm install"
    startCommand: "npm start"
